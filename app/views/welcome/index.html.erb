<table>
  <thead>
    <th>Post ID</th>
    <th>Author</th>
    <th>Content</th>
  </thead>
  <tbody>
    <% @posts.each do |p| %>
      <tr>
        <td><%= p.post_id %></td>
        <td><%= p.author.name %></td>
        <td><%= p.content %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= will_paginate @posts %>